<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.StartApp" parent="Theme.SplashScreen">

<!--       // <item name="windowSplashScreenAnimatedIcon">@drawable/gif_rl_work</item>-->

        <item name="postSplashScreenTheme">@style/Theme.RLFieldWorker</item>
    </style>

    <!-- Add a separate theme for lower Android versions -->
    <style name="Theme.StartApp.Legacy" parent="Theme.AppCompat.NoActionBar">
        <item name="android:backdropColor">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/app_logo_black</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>